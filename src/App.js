import React, { Component } from "react";
// import { Query } from "react-apollo";

class App extends Component {
  render() {
    return (
      <div className="App">
        {/* <Query query={GET_NOTES}>{() => null}</Query> // For Fix error */}
        App
      </div>
    );
  }
}

export default App;
